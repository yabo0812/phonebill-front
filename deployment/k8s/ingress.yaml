apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phonebill-front
spec:
  ingressClassName: nginx
  rules:
  - host: phonebill-dg0511-api.20.249.191.180.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: phonebill-front
            port:
              number: 8080